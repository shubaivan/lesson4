<li id="task_<%= task.id %>" class="single <%= 'done' if task.done %>">
  <%= check_box_tag 'task[done]', true, task.done,
        onchange: "$(this).data('params', 'task[done]=' + this.checked)",
        data: { remote: true, url: list_task_path(current_list, task),
        method: :patch } %>
  <%= link_to task.title, edit_list_task_path(current_list, task), remote: true %>
  <%= link_to '×', list_task_path(current_list, task), method: :delete, remote: true, class: 'delete' %>
</li>
